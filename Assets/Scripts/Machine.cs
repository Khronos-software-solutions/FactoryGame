using UnityEngine;

public class Machine : MonoBehaviour
{
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        var r = new Resource();
        r.type = ResourceType.Item;

    }

    // Update is called once per frame
    void Update()
    {
        
    }
}

